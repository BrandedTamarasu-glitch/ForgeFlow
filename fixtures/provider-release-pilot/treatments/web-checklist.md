Freeze intended asset identity before requests. Verify actual status, decoded bytes and media type for every scoped asset. Treat redirects, HTML fallback and stale mixtures explicitly. Keep unavailable evidence distinct from known failure. Observe interaction independently and retain failures regardless of observation order. Local HTTP checks do not qualify a public site or accessibility.
