# Review Context Plan

- Use bounded context packets for auth and session changes before spawning reviewers.
- Keep session token evidence local unless a reviewer requests exact source lines.

## Validation

- Rebuild the memory index before generating review packets.
