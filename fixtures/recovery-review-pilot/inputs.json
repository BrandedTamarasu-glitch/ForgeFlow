{
  "cases": [
    {
      "id": "rr01",
      "capability": "persistence-recovery",
      "code": "function publish(store) { store.head = 'g1'; }\nfunction reload(store) {\n  try { return {status:'ready', items:JSON.parse(store.blobs[store.head]).items}; }\n  catch { return {status:'recovery-required', items:[]}; }\n}\nfunction collect(store, checkpoint) {\n  const garbage = Object.keys(store.blobs).filter(id => id !== store.head);\n  checkpoint();\n  for (const id of garbage) delete store.blobs[id];\n}",
      "input": "After loading this prompt, use no tools, files, edits, or agent contact. Inspect only the supplied JavaScript and contract. Return JSON only, at most 250 words: {\"findings\":[{\"target\":\"exact function name\",\"severity\":\"low|medium|high|critical\",\"reason\":\"mechanism and impact\"}],\"reload\":null,\"limitations\":[\"...\"]}. For recovery tasks replace reload:null with the predicted reload object. Report each distinct defect once; empty findings is valid. Severity: high for loss of accepted authoritative state; medium for bounded analytics miscounts; low for minor nonblocking errors; critical only for demonstrated catastrophic impact. Missing real backend observations belong in limitations, not invented defects.\nThis is a single-threaded byte-store model: no interleaving is possible except at checkpoint; each publication or loop iteration is atomic. No other readers, writers, pins or backup-retention requirements exist. Committed items must survive cleanup and reload. Unreferenced generations may remain. Starting store is {head:\"g0\", blobs:{g0:'{\"items\":[\"old\"]}', g1:'{\"items\":[\"old\",\"accepted\"]}'}}. Execute collect(store, () => publish(store)), discard volatile state, then reload(store). Identify supported defects and predict the exact user-visible reload status/items. Do not repair the code.\nfunction publish(store) { store.head = 'g1'; }\nfunction reload(store) {\n  try { return {status:'ready', items:JSON.parse(store.blobs[store.head]).items}; }\n  catch { return {status:'recovery-required', items:[]}; }\n}\nfunction collect(store, checkpoint) {\n  const garbage = Object.keys(store.blobs).filter(id => id !== store.head);\n  checkpoint();\n  for (const id of garbage) delete store.blobs[id];\n}"
    },
    {
      "id": "rr02",
      "capability": "persistence-recovery",
      "code": "function publish(store) { store.head = 'g1'; }\nfunction reload(store) {\n  try { return {status:'ready', items:JSON.parse(store.blobs[store.head]).items}; }\n  catch { return {status:'recovery-required', items:[]}; }\n}\nfunction collect(store, checkpoint) {\n  const garbage = Object.keys(store.blobs).filter(id => id !== store.head);\n  checkpoint();\n  for (const id of garbage) {\n    if (id !== store.head) delete store.blobs[id];\n  }\n}",
      "input": "After loading this prompt, use no tools, files, edits, or agent contact. Inspect only the supplied JavaScript and contract. Return JSON only, at most 250 words: {\"findings\":[{\"target\":\"exact function name\",\"severity\":\"low|medium|high|critical\",\"reason\":\"mechanism and impact\"}],\"reload\":null,\"limitations\":[\"...\"]}. For recovery tasks replace reload:null with the predicted reload object. Report each distinct defect once; empty findings is valid. Severity: high for loss of accepted authoritative state; medium for bounded analytics miscounts; low for minor nonblocking errors; critical only for demonstrated catastrophic impact. Missing real backend observations belong in limitations, not invented defects.\nThis is a single-threaded byte-store model: no interleaving is possible except at checkpoint; each publication or loop iteration is atomic. No other readers, writers, pins or backup-retention requirements exist. Committed items must survive cleanup and reload. Unreferenced generations may remain. Starting store is {head:\"g0\", blobs:{g0:'{\"items\":[\"old\"]}', g1:'{\"items\":[\"old\",\"accepted\"]}'}}. Execute collect(store, () => publish(store)), discard volatile state, then reload(store). Identify supported defects and predict the exact user-visible reload status/items. Do not repair the code.\nfunction publish(store) { store.head = 'g1'; }\nfunction reload(store) {\n  try { return {status:'ready', items:JSON.parse(store.blobs[store.head]).items}; }\n  catch { return {status:'recovery-required', items:[]}; }\n}\nfunction collect(store, checkpoint) {\n  const garbage = Object.keys(store.blobs).filter(id => id !== store.head);\n  checkpoint();\n  for (const id of garbage) {\n    if (id !== store.head) delete store.blobs[id];\n  }\n}"
    },
    {
      "id": "rr03",
      "capability": "review-calibration",
      "code": "function prepare(store) {\n  const observed = { ...store.current };\n  return () => {\n    store.current = { count: observed.count + 1, version: observed.version + 1 };\n  };\n}\nfunction hints(store, checkpoint = () => {}) {\n  store.cacheWarm = true;\n  checkpoint();\n  store.hintReady = true;\n}",
      "input": "After loading this prompt, use no tools, files, edits, or agent contact. Inspect only the supplied JavaScript and contract. Return JSON only, at most 250 words: {\"findings\":[{\"target\":\"exact function name\",\"severity\":\"low|medium|high|critical\",\"reason\":\"mechanism and impact\"}],\"reload\":null,\"limitations\":[\"...\"]}. For recovery tasks replace reload:null with the predicted reload object. Report each distinct defect once; empty findings is valid. Severity: high for loss of accepted authoritative state; medium for bounded analytics miscounts; low for minor nonblocking errors; critical only for demonstrated catastrophic impact. Missing real backend observations belong in limitations, not invented defects.\nReview both functions under these contracts: prepare returns an accepted counter update; two callers may prepare from the same version before either returned function runs, and both accepted increments must be preserved. Each returned call executes atomically. hints updates independent boolean hints; partial progress is allowed and its caller explicitly retries a failed checkpoint before declaring both complete. No other business requirements apply. reload must be null.\nfunction prepare(store) {\n  const observed = { ...store.current };\n  return () => {\n    store.current = { count: observed.count + 1, version: observed.version + 1 };\n  };\n}\nfunction hints(store, checkpoint = () => {}) {\n  store.cacheWarm = true;\n  checkpoint();\n  store.hintReady = true;\n}"
    },
    {
      "id": "rr04",
      "capability": "review-calibration",
      "code": "function prepare(store) {\n  const observed = { ...store.current };\n  return () => {\n    if (store.current.version !== observed.version) return false;\n    store.current = { count: observed.count + 1, version: observed.version + 1 };\n    return true;\n  };\n}\nfunction publish(store, generation, checkpoint = () => {}) {\n  const next = { primary: generation, index: generation };\n  checkpoint();\n  store.current = next;\n}",
      "input": "After loading this prompt, use no tools, files, edits, or agent contact. Inspect only the supplied JavaScript and contract. Return JSON only, at most 250 words: {\"findings\":[{\"target\":\"exact function name\",\"severity\":\"low|medium|high|critical\",\"reason\":\"mechanism and impact\"}],\"reload\":null,\"limitations\":[\"...\"]}. For recovery tasks replace reload:null with the predicted reload object. Report each distinct defect once; empty findings is valid. Severity: high for loss of accepted authoritative state; medium for bounded analytics miscounts; low for minor nonblocking errors; critical only for demonstrated catastrophic impact. Missing real backend observations belong in limitations, not invented defects.\nReview both functions under these contracts: prepare returns an atomic conditional update; false rejects the attempt. Two callers can prepare before execution; the caller explicitly retries once from a fresh preparation after a conflict, with no further competitor in that retry. Both accepted increments must be preserved. publish must keep primary and index equal for every observer; reference replacement is atomic in this model and checkpoint may observe state or throw. No other requirements apply. reload must be null.\nfunction prepare(store) {\n  const observed = { ...store.current };\n  return () => {\n    if (store.current.version !== observed.version) return false;\n    store.current = { count: observed.count + 1, version: observed.version + 1 };\n    return true;\n  };\n}\nfunction publish(store, generation, checkpoint = () => {}) {\n  const next = { primary: generation, index: generation };\n  checkpoint();\n  store.current = next;\n}"
    }
  ]
}
